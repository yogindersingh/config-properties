server:
  port: 8070

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      default-zone: ${EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE:http://${eureka.instance.hostname}:${server.port}/eureka/}
